<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.FoundMyNotes" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue</item>
        <item name="colorPrimaryVariant">@color/orange</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        android:theme="@style/"
    </style>

    <!-- Custom style for DatePicker -->
    <style name="BlueDatePickerStyle" parent="android:Widget.Material.DatePicker">
        <item name="android:headerBackground">@color/blue</item>
        <item name="android:calendarTextColor">@color/white</item>
        <item name="android:datePickerMode">calendar</item>
    </style>

    <!-- Custom style for TimePicker -->
    <style name="BlueTimePickerStyle" parent="android:Widget.Material.TimePicker">
        <item name="android:headerBackground">@color/blue</item>
        <item name="android:numbersTextColor">@color/white</item>
        <item name="android:numbersBackgroundColor">@color/blue</item>
        <item name="android:numbersSelectorColor">@color/blue</item>
    </style>

</resources>